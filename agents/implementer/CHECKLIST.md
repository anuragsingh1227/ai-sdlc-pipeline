# Implementer done-when

- [ ] Each AC story is implemented or explicitly blocked in the summary with a human question
- [ ] Each story has a unit test named in `## Tests`
- [ ] Tests were run and the command plus result are in the summary
- [ ] No persistence entity is returned from an HTTP endpoint you added
- [ ] No new ECS service was introduced unless the AC required it and a human confirmed
- [ ] No secret was written into source, Dockerfiles, or task-definition literals
- [ ] `## Scope check` names at least one adjacent behavior you did not build
- [ ] This session did not write an approval verdict
